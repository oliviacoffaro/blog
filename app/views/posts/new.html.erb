<%= bootstrap_form_for(@post) do |f| %>
  <%= f.text_field :title, hide_label: true, placeholder: "Title"  %>
  <%= f.text_area :title, hide_label: true, placeholder: "Body"  %>
  <%= f.submit "Post" %>
<% end %>

<script>
$(function() {
  // Emoji picker
  $('#post_title').emojiPicker({
    height: '300px',
    width:  '450px'
  });
});
</script>
